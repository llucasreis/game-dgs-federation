type Game @key(fields: "id") @extends {
  id: ID @external
  reviews: [Review]
}

type Review @key(fields: "id") {
  id: ID
  starRating: Int
  comment: String
  game: Game
}

type Query {
  reviews: [Review]
}